# OS / editor cruft
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*.log

# Node (only if you add tooling later)
node_modules/

# Editor config (local only)
.vscode/

# Backend setup notes (contains private ntfy topic) — do not publish
backend/
